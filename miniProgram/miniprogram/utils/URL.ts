import { baseUrl } from './config'

/**
 * 访客
 */
export const visiterUrl = `${baseUrl}/visitor`

/**
 * 信息列表
 */
export const messageListUrl = `${baseUrl}/message`

/**
 * 获取圈
 */
export const circleListUrl = `${baseUrl}/circle`

/**
 * 留言
 */
export const leaveWordUrl = `${baseUrl}/leaveWord`

/**
 * 弹窗
 */
export const dialogUrl = `${baseUrl}/dialog`

/**
 * 步骤
 */
export const stepUrl = `${baseUrl}/step`
